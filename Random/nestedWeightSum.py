def helper(alist)
    total = 0
    
    def nested(alist, depth):
        count = 0
        for i in alist:
            if i == int():
                count += i
            else:
                nested(i,depth+1)
                depth-=1
        return 
    return total
    
helper([4,5,[1,2[3],9],8])
